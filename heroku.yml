setup:
  addons:
   - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile.web
release:
  image: web
  command:
    - django-migrate secomplican
run:
  web: django-collectstatic secomplican && start-server secomplican "$PORT"
