setup:
  addons:
   - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile.web
release:
  image: web
  command:
    - django-migrate src
run:
  web: django-collectstatic src && start-server src "$PORT"
