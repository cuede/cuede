setup:
  addons:
   - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile.web
release:
  image: web
  command:
    - django-migrate-and-collectstatic secomplican
run:
  web: start-server secomplican "$PORT"
