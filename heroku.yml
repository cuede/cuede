setup:
  addons:
   - plan: heroku-postgresql
  config:
    PRODUCTION: True
build:
  docker:
    web: Dockerfile.web
release:
  image: web
  command:
    - django-migrate secomplican
run:
  web: start-server secomplican "$PORT"
